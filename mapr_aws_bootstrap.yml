- hosts: localhost
  tasks:
    - assert:
        that:
          - ansible_env.AWS_ACCESS_KEY is defined
          - ansible_env.AWS_SECRET_KEY is defined

- hosts: localhost
  vars_files:
    - vars/global/main.yml
  roles:
    - mapr_aws_bootstrap

