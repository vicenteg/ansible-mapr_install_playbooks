- hosts: localhost
  vars_files:
    - vars/global/main.yml
    - vars/hosts/{{ansible_hostname}}.yml
  tasks:
    - assert:
        that:
          - aws_access_key is defined
          - aws_secret_key is defined
  roles:
    - mapr_aws_bootstrap

