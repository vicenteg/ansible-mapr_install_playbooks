- hosts: localhost
  tasks:
    - assert:
        that:
          - ansible_env.AWS_ACCESS_KEY is defined
          - ansible_env.AWS_SECRET_KEY is defined

- hosts: localhost
  roles:
    - ansible-mapr_aws_bootstrap

